<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /ElecPress/

    # Evita bucle si ya está en /public
    RewriteCond %{REQUEST_URI} !^/ElecPress/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
